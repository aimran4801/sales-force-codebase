import { LightningElement } from 'lwc';

// import { subscribe, unsubscribe, onError, setDebugFlag, isEmpEnabled }
//     from 'lightning/empApi';

export default class LightningEmailLwc extends LightningElement {
    
}